# 为什么 epoll 能支持高并发

## 结论

`epoll` 在大量连接、少量活跃连接的负载下，通过持久关注关系与就绪队列，避免每次等待都扫描全部 socket。它优化无效扫描，但不消除真实 I/O、业务执行或通知扇出成本。

```mermaid
flowchart TD
  Q["问题：为什么 epoll 能支持高并发？"]
  W["工作负载：连接数 N 很大，但活跃连接数 K 很小"]
  S["每个连接都有内核 socket 状态与读写缓冲区"]
  P["select/poll：每次等待都传入并扫描 N 个关注项"]
  PC["结果：内核与应用均有大量无效扫描"]
  R["epoll_ctl：持久保存关注集合与事件类型"]
  A["socket 与关注它的 epoll 建立反向关联"]
  E["socket 状态变化：例如接收缓冲区从空变为非空"]
  L["回调将对应项加入 epoll 的就绪队列"]
  EW["epoll_wait：直接返回 K 个就绪事件"]
  G["结果：避免每次等待扫描全部 N 个 socket"]
  D["道：预建反向索引，状态变化时只更新受影响者"]
  B1["边界：连接内存、真实 I/O 与业务 CPU 仍是成本"]
  B2["边界：多订阅者仍有通知扇出成本"]
  LC["代价：需处理删除、关闭、fd 复用与并发修改"]
  T["迁移：依赖驱动的任务调度器"]
  V["项目证据：100,000 候选中只更新 100 个后继"]

  Q -->|"先定义"| W
  W -->|"意味着"| S
  W -->|"暴露问题"| P
  P -->|"导致"| PC
  W -->|"驱动设计"| R
  R -->|"建立"| A
  S -->|"发生"| E
  A -->|"使得"| L
  E -->|"触发"| L
  L -->|"供给"| EW
  EW -->|"只消费"| G
  G -->|"抽象为"| D
  D -->|"仍受限于"| B1
  D -->|"仍受限于"| B2
  R -->|"引入"| LC
  D -->|"迁移到"| T
  T -->|"验证"| V
```

## 关键辨析

- `select/poll` 并非不知道 socket 状态；其接口没有长期保存调用者的关注关系与独立就绪队列，因此一次调用通常要处理完整的 N 项集合。
- socket 建立 TCP 连接后不会自动加入某个 epoll。应用通过 `epoll_ctl` 显式登记关注的 socket 与事件。
- 一个 socket 被许多 epoll 关注时，无需扫描所有 epoll 的关注树，但仍须通知实际订阅它的每个 epoll。这是必要的扇出工作。
